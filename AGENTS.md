# AGENTS.md

이 저장소는 AI Engineer의 사이드 프로젝트 템플릿이다.
주 용도는 `모델 실험`, `데모 개발`, `API/서빙`, `분석과 리포트`를 한 저장소 안에서 빠르게 시작하는 것이다.
상위 전역 `/Users/kimtae/AGENTS.md`를 따른다. 이 파일은 이 템플릿에 특화된 규칙만 추가한다.

## 프로젝트 목표

- 탐색용 노트북에서 끝나지 않고, 재현 가능한 실험과 실행 가능한 서비스/데모로 이어지게 한다.
- 데이터, 실험, 서비스, 데모를 한 저장소에 두되 서로의 책임을 분리한다.
- 빠른 PoC를 허용하되, 무엇이 임시이고 무엇이 운영 후보인지 구분한다.

## 기본 구조 해석

- `notebooks/`: 탐색, EDA, 빠른 검증용 노트북
- `src/`: 재사용 가능한 Python 코드
- `src/api/`: FastAPI 기반 추론/백엔드 엔트리포인트
- `src/common/`: 경로, 설정, 공용 유틸
- `data/`: 로컬 데이터 작업 공간
- `artifacts/`: 체크포인트, 인덱스, 평가 결과, 추론 산출물
- `configs/`: 실험/서빙 설정 파일
- `docs/`: 기획, 실험 메모, 아키텍처, 리포트
- `scripts/`: 반복 실행 스크립트
- `tests/`: 자동 검증 코드

## 작업 우선순위

- 데이터/실험 이슈면 먼저 데이터 경계와 평가 기준을 확인한다.
- API/서빙 이슈면 먼저 입출력 스키마, 모델 로딩, 예외 처리를 확인한다.
- 데모 이슈면 먼저 핵심 사용자 시나리오와 실패 상태를 확인한다.
- 노트북에서 검증된 로직은 가능한 한 `src/`나 `scripts/`로 끌어올린다.

## 모델 실험 규칙

- 새 모델 실험 전에는 베이스라인과 평가셋 정의를 먼저 확인한다.
- 데이터 누수 가능성이 있으면 구현보다 먼저 차단한다.
- 실험 비교는 seed, 데이터 범위, 평가 코드가 같을 때만 의미 있게 다룬다.
- 실험 결과는 `artifacts/` 또는 문서에서 다시 추적 가능해야 한다.

## API/서빙 규칙

- `src/api/main.py`를 HTTP 엔트리포인트의 기준점으로 사용한다.
- 모델 로딩, 전처리, 추론, 후처리 책임을 한 파일에 뭉개지 않는다.
- API 변경 시 요청/응답 예시와 영향 범위를 함께 설명한다.

## 데모 규칙

- 데모는 모델 성능 자체보다 사용자가 가치를 이해할 수 있는 흐름을 우선한다.
- 빈 입력, 잘못된 입력, 실패 응답을 최소한 처리한다.
- 데모용 하드코딩은 허용되지만 임시 제약으로 명확히 표시한다.

## 검증 규칙

- 기본 검증 순서는 `단위/스모크 확인 -> API 확인 -> 노트북/데모 흐름 확인`을 우선한다.
- 전체 학습처럼 무거운 작업 전에 먼저 작은 샘플로 파이프라인이 도는지 확인한다.
- 실행하지 못한 검증은 이유와 남는 리스크를 최종 보고에 남긴다.

## 템플릿 유지 원칙

- 이 저장소는 새 프로젝트의 출발점이므로 지나치게 도메인 특화된 코드로 오염시키지 않는다.
- 무거운 모델 프레임워크는 기본 설치보다 필요 시 추가하는 방향을 우선한다.
- 새 프로젝트로 복사해 갈 때 바로 쓸 수 있게 README와 폴더 의미를 유지한다.
